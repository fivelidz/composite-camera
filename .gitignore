# Android / Gradle build artifacts
*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build/
/captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab
*.ap_
*.dex

# Gradle build output (per-module)
app/build/
build/

# Kotlin
.kotlin/

# opencode / agent snapshots (not source — large, not useful in VCS)
.opencode/

# Local config that contains machine-specific absolute paths
local.properties

# Logs
*.log

# Keystore (never commit signing keys)
*.jks
*.keystore

# OS
Thumbs.db
